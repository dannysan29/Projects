{
  "dependencies": {
    "express-validator": "^7.2.0",
    "password-validator": "^5.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "name": "projects",
  "private": true,
  "workspaces": [
    "community-connect"
  ],
  "scripts": {
    "build": "cd community-connect && yarn install && yarn build"
  }
}